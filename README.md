# Wfm

Script per la pianificazione dei turni e dello straordinario con `wfm_claudegit6.py`. Il motore costruisce i turni rispettando requisiti, preferenze e pattern settimanali delle risorse.

Per vedere tutte le opzioni disponibili:

```bash
python wfm_claudegit6.py --help
```
